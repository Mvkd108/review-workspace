# ChatGPT project context

This directory is a local mirror of the ChatGPT project “GENUI HARNESS”.

- Treat every file under `sources/` as read-only reference material.
- Do not edit, rename, move, or delete synced project files.
- These files may be replaced the next time a task is created from this ChatGPT project.


## Project instructions

Before inspecting or changing implementation files, read `context/README.md` and
the context files it links. After every coherent implementation slice, update
`context/CURRENT.md`, `context/NEXT.md`, and add or amend the current dated
handoff under `context/sessions/`.

The product is a coding-agent **review workspace**, not an agent orchestrator.
Git and trusted gates are authoritative for change and merge-readiness claims.
Never delete a registered worktree implicitly.
