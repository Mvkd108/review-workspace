# Next steps

1. From `C:\Users\madha\projects\review-workspace`, start the workspace with
   `pnpm start -- --data-dir .review-workspace` and run the seven-day kill test
   described in `PHASE0A_KILL_TEST.md` across the existing Codex, Claude Code,
   and Cursor workflow.
2. Use at least ten real work units and three concurrently active worktrees.
3. Record inaccurate merge claims, unhelpful risk reasons, missed attention, and
   workflow friction daily. Fix trust or correctness defects within Phase 0a;
   do not use generated UI to compensate.
4. Decide the kill criterion from actual usage. If it passes, run the time-boxed
   AHP reducer feasibility spike. If it fails, revise or stop the repo channel.
5. Begin Phase 0b only after the AHP spike records either a pass or the explicit
   workspace-event-log fallback decision.
